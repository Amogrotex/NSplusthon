# CI: run the test suite on every push/PR. Releases should only be
# published from a green `main` (see python-publish.yml).
name: Tests

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install (editable, with dev extras)
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Byte-compile
        run: python -m compileall -q nsplusthon

      - name: Run tests
        # Tests must not import optional extras (Pillow, cryptg, isal, …)
        # at collection time — use pytest.importorskip inside the test.
        run: pytest tests/ -v --tb=short
